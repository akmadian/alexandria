spikes/
.idea/
.DS_Store
coverage.out
/dev
_assets/

# Wails / frontend build output
/build/bin
/frontend/dist
/frontend/package.json.md5

# Root binary from a bare `go build .` (wails build outputs to build/bin)
/alexandria
/generate

# Local Go workspace (gospan validation loop — CI resolves gospan from GitHub)
/go.work
/go.work.sum
