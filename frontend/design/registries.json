{
  "$description": "Non-token system registries — dispatch tables the constitution names, kept beside the tokens (NOT DTCG data). Registries, not hierarchies: add a capability = add a row.",

  "familyDirection": {
    "$comment": "§7: direction is DERIVED from the invariant, never chosen freely. The validator checks monotonicity against the resolved direction per family × theme.",
    "invariant": "interaction moves toward ink — fills darken on light themes, lighten on dark",
    "exceptions": { "cell": "raise toward white on every theme (LrC matting; §7/§20)" },
    "resolved": {
      "chrome": { "paper": "recess", "linen": "recess", "graphite": "raise", "carbon": "raise" },
      "cell": { "paper": "raise", "linen": "raise", "graphite": "raise", "carbon": "raise" }
    }
  },

  "funSites": {
    "$comment": "§17: the fun layer is injected ONLY at registered sites; one per view, area-capped, never adjacent to stage or cells, static under reduced motion. ADMISSION = the wit rule (ratified 2026-07-13): the moment must reference its own meaning — art that doesn't know what's happening fails regardless of beauty. Recipe values are §30.8 PIN; library/fun-probe.html is the pinning instrument.",
    "sites": {
      "boot-splash": { "meaning": "the Pharos greeting — the beam sweep IS the progress indicator (the lighthouse searching for your files)", "constraint": "art never costs a millisecond: duration = actual load; instant = skipped; full theatrical form only in naturally-slow moments (first-run, cold start)" },
      "hero-cta": { "meaning": "the primary call-to-action (hero rung = fill + fun overlay, §4)", "constraint": "includes the press payoff: a one-shot bloom handing off visibly to the marquee; ordinary chrome stays dry" },
      "marquee-progress": { "meaning": "the one long job the user is actively awaiting", "constraint": "the light-bar is the marquee only; dots stay the ambient progress voice" },
      "announcement-sweep": { "meaning": "something new exists here: one transient gradient lap around the thing's outline, then gone", "constraint": "runs once, never loops, never nags" },
      "slide-commit": { "meaning": "deliberate commitment rendered physically: the glass tray + puck slid to start a heavyweight/irreversible operation", "constraint": "reserved for operations whose weight warrants friction" },
      "joy-moment": { "meaning": "the §16 voids: empty states, first-run, completion, about", "constraint": "media per §16 (incl. dithered public-domain classical art, interactive dot art, illustrative skeuomorphism); never the user's photos" }
    }
  },

  "machinery": {
    "$comment": "§14: domain state → dot-matrix loader (source: dot-matrix-animations.vercel.app, 60 loaders). Loader ids PIN pending selection (§30.8). Machinery is achromatic and visible only while working (§10).",
    "map": [
      { "state": "ingesting", "category": "progress", "loader": "PIN" },
      { "state": "thumbnail-generating", "category": "spinner", "loader": "pulse-rings" },
      { "state": "preview-building", "category": "spinner", "loader": "PIN" },
      { "state": "watcher-ambient", "category": "ambient", "loader": "PIN" },
      { "state": "reconciler-working", "category": "agent", "loader": "PIN" },
      { "state": "sync-pending", "category": "status", "loader": "PIN" },
      { "state": "import-session-marquee", "category": "progress", "loader": "fun light-bar (§17, not a dot loader)" }
    ]
  },

  "tagRecipes": {
    "$comment": "Tag/badge style recipes (ratified 2026-07-13): style → which hue-scale steps, identical grammar for all 12 hues. 'outline' is the surface hybrid (border + wash) — a pure 1px colored border cannot carry a chip on light chrome. The compiler emits per-hue variables from these; components never assemble chips ad hoc.",
    "styles": {
      "tint": { "background": "color.<hue>.tint", "ink": "color.<hue>.tint-ink" },
      "outline": { "background": "color.<hue>.tint", "ink": "color.<hue>.tint-ink", "border": "color.<hue>.line" },
      "dot": { "mark": "color.<hue>.solid", "ink": "ink.1", "border": "ink.hairline" },
      "fill": { "background": "color.<hue>.solid", "ink": "color.<hue>.on-solid" }
    },
    "sizes": {
      "$comment": "Size axis (ratified 2026-07-19): placement-semantic rungs, orthogonal to style. Each binds a FULL type role — never a bare font-size (§13 type units). 'inline' rides a text line without expanding it (the line box, not the pads, is the contract); 'standard' is the chip default; 'prominent' stands alone. Pads in space quanta; runtime registry: frontend/src/components/badge. Values eye-tuned in the design-library matrix.",
      "inline": { "role": "type.micro", "padX": "space.1" },
      "standard": { "role": "type.label-sm", "padX": "space.2" },
      "prominent": { "role": "type.label", "padX": "space.3", "padY": "space.1" }
    },
    "hues": ["red", "peach", "orange", "amber", "lime", "green", "teal", "cyan", "blue", "indigo", "purple", "magenta", "gray"]
  },

  "rowIntents": {
    "$comment": "The row grammar (ratified 2026-07-13): each intent binds a height to the type roles allowed inside it — density and type step down TOGETHER or not at all (body type in a 16px row is zero-slack; dense type in a 28px row is limp). Role-in-row enforcement is usage-side (the Row primitive's API and a future usage lint), not the token validator's (clarified 2026-07-17). Density switches at section boundaries only (§8); horizontal insets identical across all intents. Control-size rhythm (D33): a control sits one rung below its row — list(24)→control-sm(20), control(28)→control-md(24), both a 2px-each-side breathe; text(16) hosts no control. Size is set explicitly per control (no cascade), so this rhythm is usage discipline.",
    "control": { "height": "size.row-control", "typeRoles": ["control", "value", "label", "placeholder"], "interactive": true },
    "list": { "height": "size.row-list", "typeRoles": ["value", "data-sm", "caption"], "interactive": true },
    "text": { "height": "size.row-text", "typeRoles": ["label-sm", "data-sm", "caption", "micro"], "interactive": false }
  },

  "icons": {
    "$comment": "§14 structural family: concept → Lucide glyph, ONE glyph per concept; components name concepts, never glyphs. Runtime registry: frontend/src/components/icon (C10-complete; the two lists grow together — compiler emission closes the gap when the count warrants). Seeded 2026-07-17: the disclosure chevron was the first per-need pull from the parked token-gaps list. Judgment concepts (rating/flag/reject) added 2026-07-18 with the §19 cell slots — fill = on (a rated star / picked flag is filled ink, §14). Machinery states use the dot-matrix map above, never rows here. Keyboard-modifier concepts (command/option/control/shift/return/delete) added 2026-07-22 with the Kbd keycap: the Mac symbols aren't in Geist Mono's subset (they mush at 11px via OS fallback), so a shortcut renders them as vector icons — crisp at any cap size.",
    "structural": {
      "check": "check",
      "disclose": "chevron-right",
      "mixed": "minus",
      "rating": "star",
      "flag": "flag",
      "reject": "flag-off",
      "folder": "folder",
      "collection": "layers",
      "tag": "tag",
      "source": "hard-drive",
      "command": "command",
      "option": "option",
      "control": "chevron-up",
      "shift": "arrow-big-up",
      "return": "corner-down-left",
      "delete": "delete"
    }
  },

  "hueEligibility": {
    "$comment": "Capability rows (2026-07-13): which scales each user-pickable role may bind. Gray is tag-only (gray signal is no signal). Accent additionally gated by the ring contract per theme — a hue failing ring Lc on any theme is not offered.",
    "tag": "all",
    "accent": { "exclude": ["gray"], "default": "blue", "gate": "contracts.json ring" },
    "attention": { "exclude": ["gray"], "default": "magenta", "gate": "contracts.json minHueDistanceDeg vs enabled labels" }
  }
}
